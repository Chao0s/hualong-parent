# Decision Log（前端修改记录）

> 用途：记录家长端原型（screens/）的修改决定，供后续调整 backend spec（docs/backend spec files/）时对照。
> 格式：每条记录 = 修改内容 + 涉及页面 + 对 backend 的影响。

---

## 2026-08-01

> **本批不是家长端评审的产物。** 来源是后端决策记录 `../hualong-backend/DECISIONS.md` 的 E 组（E1-E7）
> 及其下的 W1-W21，以及被这批决策改写的 `../hualong-backend/db/GAPS.md` G27-G31。触发链条是：
> 教师端仓库改版（`../hualong-teacher/decision.md` 10 条 + commit `e524e75`）→ 后端回冲 → 家长端被动承接。
>
> **家长端原型至今没有改版。** 本文件记录的是「后端已经拍板、家长端必须做而现在还没有做」的事项。
> 现有原型页面凡与本文件冲突者，以本文件为准。
>
> 权威顺序不变：`DECISIONS.md` > 后端 `db/01_schema.sql` > 后端 `db/DATABASE_SPEC.md` > 本文件。
> 本文件不重新定义规格、不重新定义术语，只记录家长端要承接什么。标注 **[开放]** / **[待查证]** 的
> 事项一律不许在实现时自选一种读法。

### 1. 新增一类家长任务：成长册素材征集（独立于亲子任务）

**页面**：`screens/home.html`、`screens/all-reminders.html`、`screens/evaluation-tasks.html`、`screens/parent-tasks.html`；另需新增一个素材征集提交页（文件名待定）

**修改内容**：

- 成长册素材征集是**独立于亲子任务的一类新待办**，不复用 `db_parent_task`。理由是位置：若复用亲子任务，按既有规则家长交的东西会落进成长册的「亲子活动」栏目，而它本该落在教师新增的那个栏目里。
- **粒度是栏目，不是 widget、也不是页。** 教师新增的一个成长册栏目 = 家长端**一则待办**；点进去列出该栏目**所有页面上的全部 `collected` 槽位**，一次交完。不按 widget 发（家长会一次收到六条通知，而它们其实是同一件事），也不按页发（「页」是排版概念，家长不知道册子怎么分页）。
- **齐备判定要全满**：该栏目全部槽位都有东西才算完成，不是「至少交一件」。缺件不被接受——允许缺件等于把压力与责任转嫁给教师（教师排版时就得预判哪一格家长交不出来），而且缺件必然留白，排出来的东西违反教师设计时的原意。
- **不设截止日期、没有逾期态。** 无倒计时、无到期提醒、无逾期标记，以学期结束为自然边界；催办走线下（教师当面或群里提醒）。家长端不做任何时限相关的 UI。
- **教师可代传。** 未交的槽位教师可替家长上传；提交记录以 `submitted_by('parent'|'teacher')` 区分家长提交与教师代传，家长端应能看出某一格是谁交的。
- 已知风险（三条已定决策叠加的后果，家长端不做缓解）：新增栏目不设上限 + 征集不设时限 + 齐备判定全满 ⇒ 教师多摆两个槽位就可能卡住全班出册。唯一的缓解手段就是教师代传，不另设机制。

**对 backend 的影响**：

- 数据落点：`db_book_widget`（`binding_key='collected'` 的槽位，只存教师新增栏目的 widget）+ `db_book_material_submission`（一列 = 一个槽位一名幼儿，`UNIQUE (widget_id, child_id)`）。家长端按 `section_id` 分组展示。
- 待办列表接口需按 `section_id` 返回：栏目名、该栏目 `collected` 槽位总数、当前幼儿已提交数，以及每个槽位的 `widget_id`、`widget_type`（image / text）、`grid_w × grid_h`（裁剪比例要用，见第 3 条）。
- 征集请求**不建表**：`db_book_material_request` 已否决——一个 `collected` widget 的存在本身就是一次征集，征集状态由提交记录的有无派生。
- 亲子任务提交仍走 `db_parent_task_submission`，并按 `DECISIONS.md` B1 补 `parent_id` 外键直指 `db_parent`，用于记录「哪位监护人提交的」（原先一个孩子多位监护人时无处记录，且会抢同一行）。

### 2. 素材槽位同时接受图片与文字；家长打的字与照片走同一条内容把关

**页面**：素材征集提交页

**修改内容**：

- `collected` 槽位有两型：**图片与文字**。文字槽位由家长直接在提交页打字，不是只上传照片。
- **文字上限由框的大小推导，不设全局字数上限。** 前端依该 widget 的 `grid_w × grid_h` 与当前字级即时算出 `maxlength`，打满就打不下去；溢出这件事在结构上不存在，因此也不需要缩字、裁切或报错的规则。
- 家长打的字是 UGC，**必须与照片走同一条内容把关**（后端 `CLAUDE.md` 安全红线 3、`GAPS.md` G2）。任何 UGC 在对非作者可见之前必须过一道把关，家长端不得按「提交即可见」实现。

**对 backend 的影响**：

- `db_book_material_submission` 需要文字栏 `submission_text`；文件关联走既有 `db_file_ref` 多态表，不开新表。
- **[开放]** 内容把关的状态位与人工复核队列的落点仍未拍板（`DECISIONS.md` D5、`GAPS.md` G2，级别 BLOCKER）。已定的只有方向：机审当自动闸门，只有 `risky` 与 `review` 才进人工，且复核队列必须我们自己建（微信不提供）。家长端在落点定下来之前不实现可见性逻辑。

### 3. 上传裁剪工具：比例由槽位格数决定，只存裁切成品，裁剪不可回改

**页面**：素材征集提交页

**修改内容**：

- **每个图片槽位声明一个精确的长宽比，等于该 widget 的 `grid_w : grid_h`。** 成长册网格是 15 × 24、格子 10mm 精确正方，所以 2 × 2 就是精确 1:1、6 × 4 就是精确 3:2。家长端的裁剪框直接吃这个比值。
- 家长在**上传当下**裁剪：实时预览这张照片最终成册时的取景，可拖动调位（换聊天软件头像那种操作），避免「头被切掉」到成册才发现。
- **只存裁切后的成品，不留原图。** 家长在手机上裁好、上传的就是最终版，服务器不保留原始文件。因此**裁剪是一次性的、不可回改**，家长端要把这一点讲清楚。
- 由此产生的连锁后果必须在家长端如实呈现：教师若要改某个槽位的形状，唯一的路径是**撤回该栏目的征集**；撤回会**删除该栏目已收的全部提交**（不留孤儿文件，也符合 PIPL 第六条最小必要），重新发布后家长要重新提交。系统不提供「发布了再偷偷改一下」的中间态。家长端需要「征集已撤回，请重新提交」这一态。
- 注：另有一条并存的规定——家长上传时**不强制**符合该比例，最终显示方式（Fill / Cover / 自动裁切）由教师在网格端决定。家长端按裁剪工具这条实现，不自行推翻其中任何一条。

**对 backend 的影响**：

- `db_book_material_submission.crop JSON`（裁切框）——**形态待定**。
- 撤回征集的语意是**删除提交**而不是保留，接口需明确。这套状态机的落点是 `db_growth_book_template.template_status`（`d1=draft` 草稿 | `d2=published` 已定稿），发布即冻结该栏目版面，发布后只能撤回。

### 4. 上传格式：客户端不必预转，服务器统一转 JPEG

**页面**：素材征集提交页

**修改内容**：

- **客户端传 HEIC / WebP / PNG 都接**，服务器统一转 JPEG（MozJPEG 编码，q82-85）、长边 2000px。家长端**不需要**自己转格式、不需要自己压缩、不需要限制相册可选的图片类型。
- 格式选 JPEG 与压缩率无关：**PDF 原生支持的影像编码只有 `DCTDecode`（JPEG）与 `JPXDecode`（JPEG 2000）**，WebP / HEIC / AVIF / JPEG XL 一个都进不了 PDF。存 JPEG 时嵌进 PDF 是 byte copy，零解码零重编。
- 长边 2000px 的依据：印刷 300 DPI 下满版一张图只需要 1772px，手机原图通常 4000 × 3000，等于四倍用不到的像素；与 PIPL 第六条最小必要原则同向。
- **[待查证]** 微信 `chooseMedia` 在 iOS 上回的是 HEIC 还是已由微信转成 JPEG，以及 WebP 在两端 `image` 组件的显示支持。这只影响上传端要不要自己转一道，不影响存储格式的结论；查证前不当结论用。

**对 backend 的影响**：转码发生在上传当下一次，不在渲染时。约 600 名幼儿 × 每学期 6 个征集槽 = 3600 张，原图约 10.8GB／学期，降档后约 1.8GB／学期。

### 5. 成长册查看：PDF 在家长手机上生成，不从服务器下载

**页面**：`screens/child-profile.html`、`screens/growth-book.html`、`screens/switch-child.html`、`screens/home.html`

**修改内容**：

- 入口路径照旧，但要写清楚：打开小程序 → 看到自己授权范围内的幼儿（多子女家庭经 `db_child.caretakers` 反查，`DECISIONS.md` B1 已拔掉 `db_parent_child`）→ 选一个孩子 → 看该幼儿**所有学期**的成长册（`UNIQUE(child_id, term_id)` 天然支持多学期）→ 打开某一本。
- 成长册按 A4 竖版呈现（210 × 297mm，左右边距 30mm、上下 28.5mm，内容区 150 × 240mm = 15 × 24 网格）。手机端默认宽度 best-fit 屏幕，**允许缩放**（原「手机端不允许横向滑动」的规则已被推翻），纵向可下滚。
- **PDF 在家长自己的手机上生成，不是从服务器下载。** canvas 逐页出图 → 自组极简 PDF → `wx.shareFileMessage` 直接发到聊天。服务器端只在教师批量导出时用 node-canvas 跑同一份绘图代码，家长这边零服务器渲染成本。
- **PDF 的用途要讲明白：给没装这个小程序的亲友传阅。** 150 DPI 光栅（A4 = 1240 × 1754），文字不是矢量，**不是拿去印刷店印实体册的东西**。家长端文案不得暗示可送印。
- **预览与 PDF 必须同源渲染**：家长在屏幕上看到的与发出去的必须一致，不能一套 WXML 一套 PDF 库。精度要求是像素级一致度 ≥ 95%。
- 唯一的漂移来源是字体：两端必须载**同一个字体档**（小程序侧 `wx.loadFontFace`），CJK 需子集化。**[开放]** 字体选型与授权尚未定（`GAPS.md` G29-余），家长端不得先拿系统字体凑合。

**对 backend 的影响**：单本成长册的内容按 `child_id` + `term_id` 下发（版式 + 绑定数据），渲染在客户端；服务器不为家长端生成 PDF 文件。

### 6. 教师寄语对家长可见

**页面**：`screens/growth-book.html`

**修改内容**：

- 新表 `db_teacher_message`：`content VARCHAR(300)`，**纯文本，不支持图片、视频等任何附件**，`UNIQUE (child_id, term_id)`。教师写完可回头重新编辑。
- 家长端在成长册的「教师寄语」栏目里读到它（绑定 `child.message`，300 字上限已定死，页版式库的框要按这个上限来排）。
- 后端未规定成长册之外的独立入口，家长端**不要自行假定**有一个「教师寄语」的单独页面或推送。

### 7. 成长册内容组成改为 6 项，「家长动态」被移出

**页面**：`screens/growth-book.html`；另 `docs/parent_ia.md` 需同步

**修改内容**：

- 内容组成 6 项，**固定顺序**：`intro 园所介绍 → time 在园时光 → task 亲子活动 → term 期末评估 → comp 综合评估 → message 教师寄语`。顺序固定，不提供排序。
- 相对原型的差异：**删掉「家长动态」**；原「发展评估」拆成「期末评估」+「综合评估」；「幼儿评语」更名「教师寄语」并改读 `db_teacher_message`；**「体检数据」维持已砍状态**（体检数据属 PIPL 第二十八条明列的医疗健康敏感个人信息，为一个装饰栏位新增医疗数据模组不划算）。
- **「家长动态」被移出成长册，不等于家长评价功能被砍。** `db_parent_evaluation` 仍在，月度／学期家长评价照常填写、照常查看，只是不再进纪念册。
- 「亲子活动」栏目读 `db_parent_task_submission`，所以**家长在亲子任务里提交的图文会原样进册**（绑定 `child.task`，文字上限现值 1000 字）。
- 「在园时光」栏目是**班级级素材**：教师从每条动态里挑若干张照片加入「成长资料」、活动文字全量收录，收录后全班每本册子的这一栏内容相同，不按 `child_id` 分。
- 「综合评估」栏目的五领域雷达图**不存任何文件、不存 base64**，由《3-6岁儿童学习与发展指南》124 题量表的题项分即时聚合（题项级均值），小程序端 canvas 直接画。

**对 backend 的影响**：

- `docs/parent_ia.md` 的 `BookItems` 节点仍写着旧的七项（园所介绍／在园时光／亲子活动／家长动态／发展评估／幼儿评语／体检数据），需改为上述六项。
- 成长册的可生成判定由后端算，只回布尔 `can_generate`，不回缺失明细；家长端本来也不展示缺什么。

### 8. 尚未拍板的开放项——家长端不得臆测

| 项 | 状态 | 影响家长端什么 |
|---|---|---|
| `wx.shareFileMessage` 的文件大小上限 | **[待查证]** | 页数软上限的门槛值按它定。现行 **40 页 / 16MB 只是占位**。软上限只提示不阻挡，不阻止生成 |
| 微信 `chooseMedia` 在 iOS 的返回格式、WebP 显示支持 | **[待查证]** | 上传端要不要自己转格式（见第 4 条） |
| 内容把关的状态位与复核队列落点 | **[开放]**（D5、G2，BLOCKER） | 家长提交的图文在通过把关前的可见性与状态展示（见第 2 条） |
| 字体选型与授权 | **[开放]**（G29-余） | 两端同字体是 95% 精度的前提，家长端 PDF 直接受影响（见第 5 条） |
| 不定长内容的末页不满怎么配 | **[开放]**（G29-余） | 家长看到的册子末页会不会留白 |
| `db_term_eval.eval_text` 的字数上限 | **未定**（G31） | 「期末评估」栏目的文字框要多大 |
| `db_book_material_submission.crop JSON` 的形态 | **待定** | 裁剪框数据怎么提交（见第 3 条） |
